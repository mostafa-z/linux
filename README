hey
if can't run install please run
chmod +x install.sh
sudo ./install.sh
  	#also copy files to system folder
	#copy .fonts folder to user directory

please run 
sudo apt-get install conky conky-all lm-sensors
{
created by another user
some lines added/deleted by me
}

Tested on Mint.Mate-17.1 X64
